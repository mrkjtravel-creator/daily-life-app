{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Open in Browser (Live Server)",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5500",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
